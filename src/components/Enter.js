import React from 'react';
import {
    withRouter,
  } from "react-router-dom";

const Enter = () => {
    return(
        <div>
            <h1>Enter</h1>
        </div>
    )
}

export default withRouter(Enter);